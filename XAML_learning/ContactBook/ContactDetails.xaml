<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="XAML_learning.ContactBook.ContactDetails">
    <StackLayout>
        <TableView Intent="Form">
            <TableRoot >
                <TableSection Title="Name">
                    <EntryCell x:Name="firstName" Label="First name" Keyboard="Default" Text="{Binding FirstName}"/>
                    <EntryCell x:Name="lastName" Label="Last name" Keyboard="Default" Text="{Binding LastName}"/>
                </TableSection>
                <TableSection Title="Contact">
                    <EntryCell x:Name="phoneNumber" Label="Phone" Keyboard="Telephone" Text="{Binding Phone}"/>
                    <EntryCell x:Name="emailAddress" Label="Email" Keyboard="Email" Text="{Binding Email}"/>
                </TableSection>
                <TableSection Title="Other">
                    <SwitchCell x:Name="isBlocked" Text="Blocked" On="{Binding IsBlocked}" />
                </TableSection>
            </TableRoot>
        </TableView>
        <Button Text="Save" BackgroundColor="Green" TextColor="White" Clicked="Button_Clicked"/>
    </StackLayout>
</ContentPage>